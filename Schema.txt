-- Create the main ads table
CREATE TABLE ads_data (
    ad_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    headline VARCHAR2(255),
    status VARCHAR2(20) DEFAULT 'PENDING',
    flag_reason VARCHAR2(100)
);

-- Create the policy blacklist
CREATE TABLE blacklist_keywords (
    word VARCHAR2(50) PRIMARY KEY
);

-- Seed with some "bad" words
INSERT INTO blacklist_keywords VALUES ('WIN FREE');
INSERT INTO blacklist_keywords VALUES ('CASH PRIZE');
COMMIT;